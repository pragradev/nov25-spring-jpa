spring:
  application:
    name: nov-spring-jpa

  datasource:
    url: jdbc:h2:mem:appDb
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
server:
  port: 8081
